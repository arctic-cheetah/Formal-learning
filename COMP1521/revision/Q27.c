#include <stdio.h>
#define N 10

int main(void) {
	/*
	int n, a, b, c;
	
	n = scanf("%d %d %d", &a, &b, &c);
	
	printf("%d, %d, %d, %d \n", n, a, b, c);
	*/
	char str[N] = { 'H', 'i', '\0' };
    printf("%s\n", str);
    return 0;
	
}

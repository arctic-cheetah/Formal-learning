#include "float_exp.h"

#define EXPONENT 23
// given the 32 bits of a float return the exponent
uint32_t float_exp(uint32_t f) {
    uint32_t exponent;
    
    f = f << 1;//Remove the sign
    f = f >> 1;
    
    // Remove the fraction
    f = f >> 23;
    exponent = f;
    
    return
}
